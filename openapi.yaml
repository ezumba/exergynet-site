openapi: 3.0.1
info:
  title: ExergyNet Compute
  description: Content-addressed AI memory with tamper-evident SHA-256 receipts. On-chain zero-knowledge verification is in development.
  version: "1.0.0"
servers:
  - url: https://explorer-api.exergynet.org
paths:
  /api/v1/state:
    get:
      operationId: getNetworkState
      summary: Retrieves the current thermodynamic state and latest strikes on the LNES-04 Membrane.
      responses:
        "200":
          description: OK
