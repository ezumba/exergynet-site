# External Narrative Propagation Queue — 2026-08-27

**Directive:** VP Sales Directive 004 §25, updated by Directive 005 §2/§30 with live-fetched findings.
**Status as of Directive 005:** every row below was re-fetched live this pass (not relying on the Directive 004 historical snapshot) and reclassified per Directive 005 §2's A–E scheme:
**A** = first-party controlled · **B** = derived/registry-ingested · **C** = editable third-party profile · **D** = independent third-party · **E** = historical public record (no mutation).

---

| Surface | Live-fetched finding (2026-08-27) | Classification | Desired canonical description | Stale/risk finding | Priority | Status / Directive 006 action |
|---|---|---|---|---|---|---|
| **Crunchbase** | No profile found for exergynet.org. Search + direct-fetch (403 on org pages) found only unrelated companies ("Exergyn" cleantech, generic "Exergy" energy-management org). A separate person page `crunchbase.com/person/seven-ezumba` exists but content is unverified (403 on fetch). | Unconfirmed (D or nonexistent) | N/A until a real profile is confirmed | None — nothing to correct | P3 | **CLOSED for this pass** — no profile exists to correct. Re-check periodically; Crunchbase profiles can be auto-generated later without the company's involvement. |
| **npm — `vanguard-memory-node`** | Latest 1.5.0. Description and README opening are already neutral/technical and consistent with the Gen 2 canon; no sovereign/DePIN/token language. | **A** (ExergyNet-controlled) | No change needed | None found | — | **CLOSED — already correct.** |
| **npm — `exergynet-mcp-server`** | Latest 0.2.2. Description: *"MCP server for ExergyNet LNES-04 Base Mainnet autonomous compute settlement."* Keywords include `depin`, `usdc`, `machine-to-machine`. **More seriously: the shipped code targets a retired Base Mainnet contract — see `MCP_EXTERNAL_SECURITY_REMEDIATION_2026-08-27.md` §0.** | **A** (ExergyNet-controlled, but publish blocked — no npm credentials this session) | Hold narrative correction until the §0 engineering issue is resolved (see remediation report) | Gen 0/1 framing + confirmed retired-contract usage | **P0** | **BLOCKED (credentials) + HELD (engineering).** Correction payload drafted, not applied. See `EXTERNAL_PROPAGATION_CHANGELOG.md`. |
| **GitHub — `github.com/exergynet` (org)** | **Confirmed 404 — this organization does not exist.** The site links to it from `footer.html` and `roadmap.html` (generic "GitHub" links) and `api-integration.html` (a fake `git clone .../moleculogic-prover` instruction under this nonexistent org). | **A** (first-party dead link, not a third-party issue) | Point to a real repo | Dead link on 3 live pages | **P0** (functional defect) | **CLOSED — fixed this pass.** `footer.html`/`roadmap.html` repointed to `github.com/ezumba/vanguard-memory-node`; `api-integration.html`'s fake clone instruction replaced with an honest "not yet public, request access" note. |
| **GitHub — `github.com/ezumba/exergynet-mcp-server`** | Repo description is empty at the GitHub-metadata level, but its Glama.ai listing (sourced from this repo's README) reads *"thermodynamic compute clearinghouse"* with Blockchain/Cryptocurrency tags. | **A**, write access confirmed (git credential resolves) | Same as the npm row above | Same as npm row — held pending §0 resolution | P0 | **HELD** — same reason as npm row. |
| **GitHub — `github.com/ezumba/lnes06-edge-witness`** | Repo description: *"ExergyNet LNES-06 Edge Witness - sovereign Android mesh relay, WebRTC calling, ZK sensor proofs"* — uses "sovereign" directly. | **A**, write access likely available (same account) | Drop "sovereign," describe plainly (e.g. "Android mesh relay, WebRTC calling, ZK sensor proofs") | Site-wide sovereignty purge (Directive 004) did not reach this separate repo | P1 | **OPEN for Directive 006** — not corrected this pass (deprioritized behind the more serious `exergynet-mcp-server` finding within the same research/engineering session). Repo description field itself requires GitHub API/`gh` access this session did not have (`gh auth status` → not logged in) even though git push access to repo *content* was confirmed. |
| **Glama** (`glama.ai/mcp/servers/ezumba/exergynet-mcp-server`) | Listing confirmed live and stale, sourced directly from the npm/registry `description` field (byte-for-byte match) — **not independently written**. License A, Quality B, Maintenance D. | **B** (registry-ingested) | Fixing the upstream `server.json`/npm description will propagate here on Glama's next crawl | Same Gen 0/1 framing as the source | P1 | **Fix upstream, not this listing directly.** Held pending §0 resolution (same root cause). |
| **MCP.so** | Listing confirmed live; its description is a **verbatim scrape of the GitHub README** (including raw markdown/badges), ingested automatically within ~2.6 hours of the npm package's original publish — confirmed automated crawler, not editorial. | **B** (crawler-derived) | Fixing the upstream README will propagate here on next crawl | Same Gen 0/1 framing as the source | P2 | **Fix upstream, not this listing.** Held pending §0 resolution. |
| **Smithery** | A record exists (`registry.smithery.ai/servers/ezumba/exergynet-mcp-server`) but is a content-free stub (`description:""`, `tools:null`, not deployed/hosted, not indexed in Smithery's own search). | **B**, effectively inert | No action needed until Smithery actually indexes it | None currently visible (empty) | P3 | **CLOSED for this pass** — nothing to correct in an empty record. |
| **CrossAI Tools** | Could not locate this directory at all after repeated targeted searches. Likely not a real/significant indexed MCP directory, or too obscure to surface. | Unconfirmed | N/A | N/A | P3 | **CLOSED — not found, not fabricated.** Re-check only if it resurfaces as relevant. |
| **Metatext** (`metatext.ai/tools/mcps`) | Directly checked — ExergyNet does **not** appear in this directory. | D — not listed | N/A | N/A | — | **CLOSED — no listing exists.** |
| **MCP Vouch** | Confirmed real, live scan page at `mcp-registry-dh5.pages.dev/servers/mcpr-io-github-ezumba-exergynet/`. Score 71/100, Grade C, scanned version **0.1.10** (current npm latest is 0.2.2 — four releases behind), scanned **2026-07-09**. Full per-category breakdown captured in `MCP_EXTERNAL_SECURITY_REMEDIATION_2026-08-27.md`. | **D** (independent scanner — do not attempt to alter) | N/A — the goal is a future rescan of remediated code, not an edit to this record | Score is stale relative to current npm version; underlying code has a more serious unscored issue (§0 finding) | **P0 (process)** | **OPEN, correctly not attacked or hidden.** Do not advertise the score as current. Rescan only after the §0 engineering issue and the four WARN categories are addressed. |
| **Libraries.io and other automated package mirrors** | Not independently re-fetched this pass (per Directive §10's instruction not to spend disproportionate time on auto-derived mirrors). | **B** (auto-derived) | Fixing npm/GitHub upstream is sufficient | Whatever npm/GitHub currently say | P3 | **No direct action** — will inherit whichever fix lands upstream. |
| **F6S** | Searched directly; F6S's own site search returned a bot-detection wall ("We think you might be a bot"), and general web search found no listing distinguishable from unrelated similarly-named companies. | Unconfirmed (C or nonexistent) | N/A until confirmed | None confirmed | P3 | **CLOSED for this pass — not located.** Re-check with a different access method if this becomes a priority. |
| **Paragraph** (the "ExergyNet — Physical Truth for the AI Economy" article referenced in Directive 004) | **Not found.** Multiple targeted searches (site-restricted and phrase-based) did not locate this article at any indexed URL. It may exist unindexed, under a different handle, or not have been published where expected. | Unconfirmed | N/A until located | N/A | P2 | **OPEN for Directive 006** — locate first (may require checking a specific account handle directly rather than search), then apply the editor's-note treatment from Directive 004 §12 if found. Do not assume it doesn't exist just because search didn't surface it. |
| **Affiliated site — MetalDrug (metaldrug.com)** | **Confirmed live overclaim.** Homepage and "Who We Serve" page both present "ExergyNet Secure Compute" as an operational service biotech customers can run workloads on today, citing an unsourced **"88.6% Cost reduction"** vs. centralized cloud. | **C or A** — control status not confirmed this session | Reframe to avoid presenting mock-only/testnet infrastructure as production-ready for a paying biotech customer; source or drop the 88.6% figure | Overstated production readiness + an unsourced specific benchmark number, on a customer-facing page | **P1** | **OPEN for Directive 006** — confirm whether this session's credentials extend to metaldrug.com's repo/CMS before editing; if yes, correct; if no, record as a coordination item like the KTX/affiliated-entity row below. |
| **Affiliated site — MyMonitor.ai** | One mention found: team/company page lists Seven Ezumba as "Co-Founder & CTO," bio line *"including work as the architect of ExergyNet, MetalDrug, and TensileEra."* Biographical, not an ExergyNet capability claim. Also surfaces a **new affiliated project name not previously tracked: "TensileEra."** | D/A (biographical mention on a site of unconfirmed control) | No change needed — accurate as a biographical reference | None — flagging "TensileEra" as a new name for the affiliated-entity inventory | P3 | **CLOSED — no correction needed.** Add "TensileEra" to the affiliated-entity watchlist for future passes. |
| **Optimism governance record** | **Confirmed via raw Discourse data.** Season 9 Final Report, Growth Applications table: *"ExergyNet — 50,000 OP — Declined by GC."* Forum report gives no specific written reason for decline, only the outcome. Posted 2026-05-23 by user Gonna.eth. An associated `app.opgrants.io` application page could not be loaded (JS-rendered, fetch failed) to check for an attached reviewer comment. | **E** (historical record — no mutation) | N/A | N/A — this is disclosure, not a defect | P1 (diligence, not correction) | **Diligence entry filed** (see below) — record treated as untouchable per Directive §14. Follow-up: someone with a real browser could check `app.opgrants.io` directly for a possible reviewer comment not visible via automated fetch. |
| **KTX / Kunfirm and other Ezumba Dynasty Trust-affiliated public pages** | Not inventoried this pass beyond MyMonitor.ai (see above) — no dedicated KTX public page was located during this pass's research. | Unknown | Must carry the dual-boundary framing from `machines.html` if any such page exists | Unknown until inventoried | P1 | **OPEN for Directive 006** — remains a coordination task, not a copy-edit task, per Directive 004's original framing. |

---

## Optimism governance diligence entry (per Directive §14)

**Fact:** An ExergyNet grant request for 50,000 OP tokens appears in the Optimism Collective's Season 9 Final Report (Growth Applications table) and is marked "Declined by GC" (Grants Council). Source: `gov.optimism.io/t/season-9-final-report/10685`, posted 2026-05-23.

**What it does not establish:** the public record as fetched contains no stated reason for the decline. It does not by itself establish that ExergyNet's technology failed, that its architecture was rejected on technical grounds, that anything fraudulent occurred, or that ExergyNet received funding from any other source. A decline from one grants program, for reasons not stated in the fetched record, is the entire confirmed fact.

**Handling:** not proactively promoted; not concealed if asked. No further action taken on the record itself.

---

## 2026-08-28 update (Directive 007/008) — do not overwrite the rows above; this supersedes their status only

The rows above are left as the historical record of what was true on 2026-08-27. What has changed since:

| Surface | What changed | Current status |
|---|---|---|
| **npm — `exergynet-mcp-server`** | 0.2.4 published (independently verified: `latest`, ordinary install resolves to it, retired address absent, `exergynet_open_job` fails closed). 0.2.0–0.2.2 deprecated. Source for a follow-up, 0.2.5 (fixes remaining `npm audit` findings via a targeted override, no breaking downgrade), is on `main`; npm publication of 0.2.5 is still blocked on operator credentials. | **P0 resolved for 0.2.4.** 0.2.5 publish is a lower-priority open item, not a P0. |
| **Official MCP registry** (`io.github.ezumba/exergynet`) | Confirmed still serving 0.1.10 metadata that lists `BASE_PRIVATE_KEY` as *required* — worse than the "stale" framing above, this is actively wrong about what the current package needs. Root cause fully identified: the real tool is `mcp-publisher` (GitHub-release binary, confirmed current v1.8.1), gated behind the operator's own GitHub OAuth device-flow. `server.json` in the repo is already correct and publish-ready. | **P0 (was implicitly folded into the npm row above; splitting it out because the fix path is entirely different — GitHub OAuth, not npm login).** Blocked on operator authentication, not engineering. |
| **Glama** | Re-examined in more depth, not just confirmed-stale: it actively describes the pre-fix autonomous-USDC-approval behavior as a working feature and instructs setting a real `BASE_PRIVATE_KEY`, with zero advisory mention. Its Schema tab is *even more* stale than its Overview tab (references "LNES-01," an older name than the Overview's "LNES-03") — the listing is a patchwork of multiple crawl vintages, not one snapshot. **New finding: this is not a low-traffic dusty listing** — a same-day search-footprint check (see below) found Glama as the #1 organic result for "ExergyNet," "ExergyNet AI," "ExergyNet MCP," and "ExergyNet Seven Ezumba," and search engines' AI-generated summaries for all four queries directly repeated Glama's retired-write-path framing verbatim. | **Priority raised from P1 to P0.** This is the single highest-leverage external-description fix available — fixing the upstream repo (already done) does not appear to be enough on its own; Glama requires a claim (GitHub-identity-gated "Claim" button) to force a recrawl/correction, which is an operator action. |
| **MCP.so** | Same substantive finding as Glama (FAQ explicitly states the tool "handles on-chain transactions autonomously... without requiring human confirmation," no advisory mention). **New finding: its `/submit` page is a $39 paid new-listing/premium-placement flow, not a correction mechanism — do not use or pay for it.** The correct free path is the "Verify ownership via GitHub to manage this listing" button on the existing listing itself. | Priority unchanged at P1/P2, but the *correction path* is now precisely identified and it is free — no budget line needed. |
| **Smithery** | New finding: the listing page carries a banner reading "Smithery joins Arcade" linking to `arcade.dev/blog/smithery-joins-arcade`, suggesting Smithery is being folded into Arcade.dev. This likely explains the persistent empty stub better than "not yet indexed." | Recommend leaving alone, per Directive 008 §6's own default guidance for a listing that isn't meaningfully indexed — populating a listing on a platform mid-acquisition is a poor use of effort. |
| **MCP Vouch** (`mcpvouch.com` → `mcp-registry-dh5.pages.dev`) | The self-service rescan command the site itself documents, `npx mcpr scan`, does not resolve — the `mcpr` npm package 404s and its linked GitHub repo (`Incultnitollc/mcp-registry`) also 404s. Broad web search for "MCP Vouch" found zero independent mentions anywhere outside this one site, reinforcing that it is a small/low-recognition service, not an industry-standard reference. | **Downgraded from "P0 process item, rescan when code is ready" to `INDEPENDENT_RESCAN_UNAVAILABLE`.** See replacement finding below. |
| **Replacement independent scanner — Snyk Agent Scan** (new) | Verified real and current: `github.com/snyk/agent-scan` (this is the same lineage as "Invariant Labs' `mcp-scan`," which Snyk acquired — the old GitHub path 301-redirects to this repo). ~3,000 stars, pushed the same day as this research, open-source scan methodology, backed by a well-known, established security vendor. Distributed via `uvx snyk-agent-scan` or a signed standalone binary — **not** via npm. Running any scan requires the operator's own free Snyk account and API token (`SNYK_TOKEN`) — this session cannot create that account or run the scan. | **New open item, not yet executed.** Exact operator command prepared in `MCP_DISTRIBUTION_PROPAGATION_2026-08-28.md`. |
| **First-party finding, new: `exergynet.org/mcp.html`** | This site's own MCP documentation page describes a *different*, HTTP+Ed25519-signed "MCP Gateway" (distinct from the `exergynet-mcp-server` npm package) with an `exergynet_open_job` method that "locks SOL escrow," including example code instructing an agent to sign the request with its Solana private key. **Verified this endpoint is not actually live**: `POST https://exergynet.org/mcp` returns a generic static-server 405, and `GET` just serves the same HTML documentation page — there is no real backend. The page's existing "testnet mode" notice does not disclose that this specific documented flow is unimplemented. | **New P1 finding, first-party, outside the original npm-package incident scope.** Not corrected this pass — flagged for the operator to decide: label the flow clearly as unimplemented/planned, or remove it. This is a content/product decision, not a copy-edit, and describes the same autonomous-signing pattern this whole incident chain has been correcting elsewhere. |
| **`docs.html` — broken link** | `https://explorer-api.exergynet.org/command.html` returns 404; the bare domain root also 404s. Not corrected this pass (needs to know the intended real target). | New P2 functional-defect finding. |
| **Site-wide Discord link inconsistency** | Nav/footer across almost every page link to `discord.com/channels/{guild}/{channel}` — a permalink format that requires existing server membership and does not function as a public join flow for a new visitor. Only `connect.html` uses the correct public invite format, `discord.gg/EXERGYNET`. | New P2 funnel finding — a real, if quiet, conversion leak on the primary community CTA used almost everywhere. |

### Search-footprint re-run (2026-08-28), compared against the queries above

Queries run: "ExergyNet", "ExergyNet AI", "ExergyNet MCP", "ExergyNet state memory persistent", "ExergyNet Seven Ezumba", "ExergyNet ZK sovereign DePIN". Findings:
- Glama (`glama.ai/mcp/servers/ezumba/exergynet-mcp-server`) is the #1 organic result for every query that returned an ExergyNet-specific result, and search engines' AI summaries repeat its retired-write-path framing nearly verbatim each time. This is the clearest evidence yet that fixing Glama specifically is high-leverage, not just principle.
- "ExergyNet state memory persistent" and "ExergyNet ZK sovereign DePIN" returned **zero** ExergyNet-specific results — both the new state/memory canon and the old sovereign/DePIN/ZK-marketplace framing (which Directive 008 §12 asks to avoid) are currently invisible to search under those terms. Net: no urgent external correction needed for the legacy framing (it simply isn't indexed that way), but the new canon also hasn't propagated yet — there is nothing to defend and nothing to correct on this specific axis yet.
- `crunchbase.com/person/seven-ezumba` exists as an indexed profile but is behind a Cloudflare bot-detection challenge; not inspected (bypassing bot detection is out of scope for this session). Worth a manual check by the operator.
- Multiple unrelated people/companies share close-enough names ("Seven Ezumba" the basketball player; "Exergyn," "Exergy Labs," "Exergy Systems," "Exer AI" as unrelated companies) — any future bio or company-name material should account for this disambiguation risk.

## Governing rules carried forward from Directive 004 (unchanged)

1. No silent history rewrites for first-party publishing accounts (Paragraph, npm README) — a correction is a new dated entry or a clearly-labeled edit.
2. Crawl-based directories (Glama, MCP.so, Smithery, Libraries.io) are fixed at the source, not the listing.
3. MCP Vouch and the Anthropic/MCP-registry claim are process items requiring an underlying engineering or verification action, not a copy edit.
4. Cross-entity surfaces (KTX/EDT-affiliated pages, Optimism governance) require coordination or read-only diligence, never unilateral editing.
